# Path to cTrader CLI binary on THIS machine (must be runnable here).
CTRADE_CLI_PATH=/path/to/ctrader_cli

# How many parallel backtests to run in this worker.
OPTIMO_WORKER_PARALLEL=4
# If OPTIMO_WORKER_PARALLEL=auto or unset, default worker slots are
# cTrader-like (floor(cpu_cores / 2) + 1), then multiplied by this value.
OPTIMO_WORKER_PARALLEL_PER_CORE=1

# Where the worker stores run workdirs (reports/logs/tmp).
OPTIMO_WORKER_ROOT=/var/lib/optimo-worker/runs

# Repo update settings (used by optimo-worker-update.sh)
OPTIMO_WORKER_REPO_DIR=/opt/workerC
OPTIMO_WORKER_GIT_REMOTE=origin
OPTIMO_WORKER_GIT_BRANCH=main
